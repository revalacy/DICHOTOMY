var roundmsg;
var gms = newval.replace(value, 'and ' + value).split(',').join(', ');
if (gms.split(',').length === 2) gms.replace(',','');
      
      //run message send for message array
      messagehandler(messages, roomchannel);